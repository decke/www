kind: pipeline
name: default

clone:
  disable: true

steps:
- name: clone
  image: docker:git
  commands:
  - pwd
  - git clone https://code.bluelife.at/decke/www.git /drone/src
  - pwd
  - git checkout $DRONE_COMMIT
  - pwd
  - find .

- name: build
  image: cbrgm/drone-hugo
  settings:
    hugo_version: 0.55
    pull: always
    url: https://www-dev.bluelife.at/
    validate: true

- name: deploy
  image: lucap/drone-netlify
  settings:
    token:
      from_secret: netlify-token
    site_id:
      from_secret: netlify-siteid-dev
    domain: www-dev.bluelife.at
    path: ./public
